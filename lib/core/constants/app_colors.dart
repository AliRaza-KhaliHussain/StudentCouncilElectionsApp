import 'dart:ui';

class AppColors {
  // ─────────────────────────────────────────────
  // 🌿 Primary Theme Color
  static const Color primary = Color(0xFF26A62A); // Green

  // ─────────────────────────────────────────────
  // 🌗 Backgrounds & Cards
  static const Color lightBackground = Color(0xFFF8FFF8);
  static const Color lightCard       = Color(0xFFFFFFFF);
  static const Color darkBackground  = Color(0xFF121212);
  static const Color darkCard        = Color(0xFF1E1E1E);

  // ─────────────────────────────────────────────
  // 🖋 Text Colors
  static const Color lightText = Color(0xFF333333);
  static const Color darkText  = Color(0xFFE0E0E0);

  // ─────────────────────────────────────────────
  // 🟢 Semantic Colors
  static const Color success = Color(0xFF2E7D32); // Green
  static const Color error   = Color(0xFFC62828); // Red
  static const Color info    = Color(0xFF1976D2); // Blue
  static const Color warning = Color(0xFFFFA000); // Amber

  // ─────────────────────────────────────────────
  // 🔳 Neutral Colors
  static const Color white      = Color(0xFFFFFFFF);
  static const Color black      = Color(0xFF000000);
  static const Color lightGrey  = Color(0xFFBDBDBD);
  static const Color darkGrey   = Color(0xFF424242);
  static const Color borderGrey = Color(0xFFE0E0E0);

  // ─────────────────────────────────────────────
  // 🧊 AppBar & Accents
  static const Color appBar        = Color(0xFF00838F); // Teal for Blockchain screen
  static const Color accentYellow = Color(0xFFFFEB3B);  // Optional accent
}
